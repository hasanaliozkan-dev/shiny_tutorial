#result matrix
for (i 1:length(df)) {
    #temparray #one dimension
    for (j 1:length(df)) {
        #öklid hesapla 
        #öklid(df[i],df[j])
        #temparray  <- append et       
    }
    #result matrix append et temparray
    #temparray sıfırla
}
#heatmap çizdirceksin 

function(compare,df ){
    #ilk başta parametrelerin sayısına bakcaksın 
    #compare = other
    #öklid mesafesini al sonrasında heatmapi çizdir
    
    #compare = self
    #ilk sıradaki parametreyi alıp for döngüsüne sokcan. 
    #ikinci sıradaki parametreyi al fora sok.
    #result  <-  result1 -result2
    #tekrar heatmap çizdirip görselleştireceksin. 

}